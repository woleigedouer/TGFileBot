# Telegram 文件流机器人 环境变量范本（中文注释）
#
# 使用说明：
# 1) 将本文件保存为 .env。
# 2) 填写下方配置中的占位内容（不要保留尖括号 <>）。
# 3) 生产环境请妥善保管本文件，切勿提交到公开仓库。
#
# -----------------------------------------------------------------------------
# 基本配置（必填）
# -----------------------------------------------------------------------------

# Telegram API 信息：在 https://my.telegram.org/apps 申请
API_ID=<你的API_ID>
API_HASH=<你的API_HASH>
TELE_ID=<创建BOT的用户ID>

# 机器人 Token：在 @BotFather 创建机器人后获取
BOT_TOKEN=<你的_BOT_TOKEN>

# 日志频道 ID：机器人会将处理结果、转发内容等发送到这里
# 说明：
# - 必须是以 -100 开头的长整型ID，例如 -1001234567890
# - 需要先把机器人加入该频道并授予管理员权限
LOG_CHANNEL=<你的日志频道ID以-100开头>

# -----------------------------------------------------------------------------
# User Bot 配置（可选）
# -----------------------------------------------------------------------------
# 是否启用 User Bot（使用你的个人账号读取频道消息，以提升兼容性）
# 启动后用 /phone 命令设置（将加密保存到本地 phone.enc）

# -----------------------------------------------------------------------------
# 服务器与链接配置（可根据需要调整）
# -----------------------------------------------------------------------------
# 服务监听端口（默认 8080）
PORT=8080

# 外部可访问的服务地址（必须可被用户访问）
# 示例：
# - 局域网测试:  HOST=http://192.168.1.100:8080
# - 生产：       HOST=https://yourdomain.com
HOST=http://127.0.0.1:8080

# 直链中的哈希长度，用于简单校验与防止滥用（建议 6～12）
# 示例：当 HASH_LENGTH=6 时，直链形如 /stream/12345?hash=abcdef
HASH_LENGTH=6

# -----------------------------------------------------------------------------
# 权限控制（可选）
# -----------------------------------------------------------------------------
# “管理员列表”，可使用 /allow 与 /disallow 命令。
# “管理员列表”，可使用 /allow、/disallow、/phone 命令。
# 示例：ADMIN_USERS=123456789,987654321
ADMIN_USERS=

# -----------------------------------------------------------------------------
# 获取所需参数的小贴士
# -----------------------------------------------------------------------------
# 1) LOG_CHANNEL（频道ID）
#    - 将机器人加入你的频道并设为管理员
#    - 在 Telegram 搜索并使用 @getidsbot 或 @JsonDumpBot，转发频道消息以获取 chat_id
#    - 一般频道ID为负数，形式如 -100xxxxxxxxxx
#
# 2) API_ID / API_HASH
#    - 前往 https://my.telegram.org/apps 申请（需要 Telegram 账号）
#
# 3) BOT_TOKEN
#    - 在 @BotFather 创建机器人并获取 token
#
# 4) PHONE_NUMBER（User Bot）
#    - 请使用含国家区号的手机号，例如 +8613800138000
#
#    - 也可留空，启动后私聊机器人发送 /phone +8613xxxxxx 设置（将加密保存在 phone.enc）
# 安全与运维建议
# -----------------------------------------------------------------------------
# - 勿将本文件提交到公开仓库，防止泄露 API_HASH/BOT_TOKEN/手机号等敏感信息
# - 生产环境建议改用系统环境变量（例如 Docker/系统服务中注入）
# - 定期轮换 BOT_TOKEN，及时撤销不再使用的密钥
# - 备份 local.db 会话数据库，避免频繁登录触发限制
# - 若更换 API_HASH/BOT_TOKEN/TELE_ID，历史 phone.enc 将无法解密，需要重新设置 /phone
# - 备份会话数据库（如 fsb.session、userbot.session），避免频繁登录触发限制
